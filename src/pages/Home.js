import CanvasComponent from "../components/Object/Canvas";

const Home = () => {
    return (
        <main style={{ height: "100vh" }}>
            <CanvasComponent />
        </main>
    );
};

export default Home;
